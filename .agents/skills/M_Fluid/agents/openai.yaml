interface:
  display_name: "M Fluid"
  short_description: "Repo-specific patterns and workflows for M_Fluid"
  default_prompt: "Use the M_Fluid repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true